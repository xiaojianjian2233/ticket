@苗一琳  根据源码分析，我已经找到了 `canBeDeduction=0` 的具体原因。

【根因分析】
`DeductionUtils.canBeDeduction()` 对这张票返回 `false`，原因是：

该发票的商品明细中 `goodsName` = `*交通运输服务*客运服务费`，但代码判断可抵扣运输服务的逻辑是检查 `items` 字符串是否包含 `*运输服务*`（见 `CanBeDeductionEnum.java:101`）。由于商品名称中 `交通` 二字插在 `*` 和 `运输服务` 之间（即 `*交通运输服务*` 而非 `*运输服务*`），**子串 `*运输服务*` 不匹配**，导致 `hasTransportService` 判断为 false。

同时，计算可抵扣时传入的 JSON 数据中**缺少 `specialTypeMarkName` 字段**（该字段未从上游传递下来），导致 `isPassengerTransport` 也为 false。两者均为 false → 代码返回 false → `canBeDeduction=0`。

【证据】
- 源码：`CanBeDeductionEnum.java:87-102`（`api-invoice-input-utils`）
  - `specificBusType`（来自 `specialTypeMarkName`）为空 → `isPassengerTransport=false`
  - `items.contains("*运输服务*")` 在 `goodsName=*交通运输服务*客运服务费` 上返回 false → `hasTransportService=false`
  - 两者均为 false → 返回 false
- 日志：`api-invoice-check` 服务 2026-06-09 15:11:34
  ```json
  "计算是否可抵扣[...results[false]"
  ```
  传入数据中不含 `specialTypeMarkName`，`items` 为 `*交通运输服务*客运服务费`

【解决建议】
该发票本质是滴滴出行的旅客运输服务（出租车），属于可抵扣范围，但 `specialTypeMarkName` 未被传递到可抵扣计算环节，且商品名称 `*交通运输服务*` 未命中 `*运输服务*` 的字符串匹配规则。建议联系研发确认是否需要：
1. 在可抵扣计算入口补传 `specialTypeMarkName` 字段
2. 或将商品名称匹配规则调整为更灵活的方式（如 `contains("运输服务")` 而非 `contains("*运输服务*")`）

以上分析是否帮助您解决了问题？请回复 1 解决  2 未解决。

👉 如还有疑问，可直接回复本消息




@王昀有  找到了完整的 websocket 推送链路。以下是分析结论。

发票号码 `26958479111040042866` 的 websocket 推送已完成，推送结果为成功。

【证据】
- 日志：TraceId `9eeabbd69d25bdab`，时间 2026-06-10 10:49:07，服务 `fpzs`
- 推送接口：`/m4/fpzs/expense/polling/invoice/storage`，事件类型 `push-invoice`
- `FpzsPushDataController` 返回结果：`{"errcode":"0000","description":"成功"}`
- fpzs 通过 Feign 调用 `api-push-service` 的 `/link/server/message/push` 接口进行推送
- 该发票总共被推送了多次（6月6日、6月8日、6月9日、6月10日），每次均返回成功

【发票信息】
| 字段 | 值 |
|------|------|
| 发票号码 | 26958479111040042866 |
| 发票类型 | 数电票（航空运输电子客票行程单） |
| 流水号 | d47d1eaf8c1a40cba6c1c3c8de6f04f20 |
| 销方 | 深圳航空有限责任公司 |
| 购方 | 山东欣宏药业有限公司 |
| 金额 | 889.90 元 |
| 税额 | 80.10 元 |
| 价税合计 | 1,020.00 元 |
| 税率 | 9% |
| 查验状态 | 通过 |
| bxd_key | 2245095154778139648_ERX_6a28cff0fa9d8a |

> 以上分析是否帮助您解决了问题？请回复 1 解决  2 未解决。

👉 如还有疑问，可直接回复本消息


请问访问页面时具体看到了什么错误信息？比如：

1. 页面显示的报错内容或错误码是什么？
2. 是否有对应的 **traceId** 可以查询链路日志？
3. **影像系统**具体是指发票云的影像管理模块，还是独立的影像服务平台？

提供这些信息我可以进一步通过日志定位根因。

👉 如还有疑问，可直接回复本消息




问题：收票助手界面需要新增一个更换logo的需求
答复：
根据 issue-diagnosis 流程 Step 1 判断：

您描述的是一个**新增需求**（收票助手界面更换logo），而非具体的报错或异常问题。当前技能定位为**问题诊断与根因定位**，需要提供以下信息才能执行排查：

- **traceId**（服务链路追踪ID）
- **错误关键词**、异常堆栈、报错文本
- **问题发生的服务名或接口名**
- **时间范围**

如果您遇到了与收票助手相关的具体报错（如页面报错、接口异常、功能异常等），请提供上述信息，我将按流程进行诊断分析。

👉 如还有疑问，可直接回复本消息



问题：收票助手换logo的时候点不动

答复：
您使用的环境是哪个？（生产/测试/演示）

1. 生产环境 - 正式使用环境
2. 测试环境 - 开发测试环境
3. 演示环境 - demo/演示环境
