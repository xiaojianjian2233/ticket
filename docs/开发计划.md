# ticket-hub 开发计划（执行路线图）

> 编制：2026-06-10 · 依据：需求基线v2 / 业务规则10条 / 技术架构 / Skill详规 / DDL(23表) / 后端接口清单 / 前端规划。
> 目标：完成全部前后端业务代码 + Gstack 测试，服务器部署、全流程跑通，形成部署安装包。
> 生产红线见 memory/production-constraints：**KSM 只调一次不重试**；**Linear 责任人只能李志坚**；开发期 **WRITEBACK_DRY_RUN=true**；部署经 nginx `http://dl.piaozone.com:18025`。

## 进度图例：✅完成 🟡进行中 ⬜未开始

---

## 阶段0 · 环境与地基（前置）
- ✅ 远程 PG `ticket_hub` 建库（共享 PG `docker-db_postgres-1`）；**pgvector 装不了(PG18 apk 与 PG15 容器 ABI 不兼容)→改 jsonb+应用层余弦**
- ✅ ddl.sql 应用：23 表 + 26 序列全部创建成功（jsonb embedding）
- ✅ 生成 `.env`（映射 env.txt，变量名按 config.py；agent单步/KSM单调retry=0/Linear责任人李志坚/dry_run/CORS），config 已扩展并验证加载
- ✅ 数据层：23 ORM + 队列(SKIP LOCKED) + enums/constants（Py3.9 验证）
- ✅ 入站骨架：webhook 入队 + worker 循环 + harness run_step + 智齿入库链路

## 阶段2 集成层进度（部分完成，真实调用验证）
- ✅ base.py(httpx 超时/退避重试/dry-run/错误归一；KSM retries=0)
- ✅ llm.py(OpenAI兼容 chat) + ai/llm_gateway(Claude→DeepSeek)：**真实调用通过**(claude-sonnet-4-6)
- ✅ siliconflow_client(bge-m3)：**真实调用通过**(1024维)
- ✅ agent_client(旧单步, 防御解析, retries=0)
- ✅ common/utils/vector.py(余弦/top_k)
- ⬜ zhichi/ksm/linear/feishu/minio client

## 阶段1 · 全局公共代码/配置（阶段3要求，提前夯实）
- ✅ 后端：统一响应 response、全局异常 exceptions、中间件、JWT/RBAC security、结构化日志 logging、config
- ⬜ CORS 中间件（CORS_ALLOW_ORIGINS）
- ⬜ common/utils：时间(北京时区/工作日)、文本(HTML清洗)、加密(md5签名)、cosine
- ⬜ APScheduler 配置 + scheduler.py 进程骨架
- ⬜ requirements.txt 补全（已含核心；加 apscheduler 已在）

## 阶段2 · 集成层 integrations/（最优先：流水线地基）
- ⬜ base.py：httpx async 封装（超时分级/重试退避/token缓存/降级钩子/写 t_integration_log）。**KSM 走 max_retry=0**
- ⬜ llm_gateway（ai/）：Claude→DeepSeek 降级；claude_client(piaozone网关)/deepseek_client
- ⬜ siliconflow_client：bge-m3 embedding
- ⬜ agent_client：⚠️按现有**旧单步** `POST INTERNAL_AGENT_URL {question,cid}`（四步无凭证）；输出 answer + transfer_result
- ⬜ zhichi_client：get_token(md5签名,缓存)/get_ticket_by_id/save_ticket_reply/upload_file
- ⬜ ksm_client：三步鉴权 + subscribeCallback/lock/handle/supply（**单次调用**）
- ⬜ linear_client：issueCreate(team固定,assignee=李志坚)/issueUpdate/commentCreate
- ⬜ feishu_client：OAuth(SSO) + webhook 通知发送
- ⬜ minio_client：put/get/public_url（服务器端连 minio:9000）

## 阶段3 · AI 执行层 ai/
- ⬜ skill_runner：从 t_skill_md 读 SKILL.md（缓存+version失效）喂 LLM，信封出参
- ⬜ skills（py）：faq_retrieve(硅基检索本地t_faq>0.8)、desensitize(两类脱敏)、answer_extract(两段)
- ⬜ scripts/seed.py：9 个 SKILL.md → t_skill_md；t_module_owner(产品线/模块/责任人)种子；t_dispatch_assignee/config；t_holiday

## 阶段4 · 流水线 pipeline S1~S13（核心业务）
- ⬜ S1 ticket-routable（流转，不接管按来源处置；退回单末端转人工）
- ⬜ S2 ticket-tagging（产品线+模块 LLM选，dev_owner 查 t_module_owner）
- ⬜ S3 info-dedup 短路（硅基 top8 + LLM 0.85，命中复用历史答复关单）
- ⬜ S4 faq-retrieve（命中关单）
- ⬜ S6 agent-answer（旧单步）
- ⬜ S7 answer-router（transfer_result→C / LLM 分 A/B/C/D）
- ⬜ A: hub-dedup(0.7)→linear-sync(assignee李志坚)；B/C: dispatch 派单转人工；D: reply-humanize→desensitize→writeback→faq-record→faq-review
- ⬜ writeback_service（KSM handle/智齿 reply；dry_run）+ resolved_at
- ⬜ KSM 入站 handle_ksm_intake（subscribeCallback 单次 + normalize_ksm）

## 阶段5 · 周期任务 sla/ + scheduler
- ⬜ workday(工作日/节假日 t_holiday)、sla_scanner(双SLA 每日08:00)、observe_scanner(14天closed)、queue_monitor、holiday_service

## 阶段6 · 其余模块 router/service/repository/schema
- ⬜ feishu(SSO登录 auth + notify_service)、ticket(列表/详情/handle/close/tag/rejudge/requeue)、knowledge(FAQ CRUD/检索/审核)、hub、sla(查询)、assistant(NL2SQL + sql_guard + 提单)、skill(SKILL.md 管理/回滚/preview)、dispatch(配额名单)、module-owner、users
- ⬜ /webhook/linear 回调分派（流程10）
- ⬜ main.py 装配全部路由

## 阶段7 · 前端（Vue/React，沿用参考栈）
- ⬜ 全局：请求封装(401/403/拦截)、路由+守卫、状态管理、权限字典、公共组件、状态标签字典
- ⬜ 页面：登录 / 工单列表·详情·未接管·工作台 / 研发单 / FAQ管理·查阅·审核 / SLA监控 / 智能助手 / 系统配置(用户/Skill/派单/模块映射/告警)
- ⬜ 前后端环境配置

## 阶段8 · 测试（Gstack 浏览器自动化）
- ⬜ 后端单测/集成测试（pytest，连 ticket_hub）
- ⬜ 逐模块 Gstack：用例(正常/分支/边界/异常)+操作步骤+元素定位+断言+可运行脚本
- ⬜ 端到端串联：智齿入站→流水线→各分支→回写(dry_run) 全链路

## 阶段9 · 校验/修复/联调优化
- ⬜ 语法/依赖/配置校验、业务补全、第三方重试降级核对(KSM单次/Linear李志坚)、前端交互、Gstack 稳定性、日志&性能

## 阶段10 · 部署 + 服务器全流程测试 + 安装包
- ⬜ Dockerfile + docker-compose(backend+worker+scheduler+minio；PG复用共享)
- ⬜ 部署到服务器、nginx 代理(`http://dl.piaozone.com:18025`)
- ⬜ 服务器端全流程 + Gstack 浏览器级测试
- ⬜ 形成部署安装包（结束标志）

---

## 执行顺序（最优）
阶段0/1 收尾 → **阶段2 集成层** → 阶段3 AI层+种子 → **阶段4 流水线**(打通主业务) → 阶段6 后端API → 阶段5 定时 → 阶段7 前端 → 阶段8 测试 → 阶段9 修复 → 阶段10 部署。
每步本地 py_compile + venv import + 能连 PG 的逻辑用真库验证；MinIO/webhook 相关在服务器验证。
